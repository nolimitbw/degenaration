export const AUTOMATED_MAINNET_RELEASE = Object.freeze({
  enabled: false,
  network: "solana-mainnet" as const,
  label: "Solana Mainnet",
  reason: "Bots can be built and saved now. Running them automatically is not yet available, so no bot can move funds on its own."
});
