# AI handoff

The single place either agent reads before starting work. **Update it when you stop.**

Last updated: 2026-07-30 by Claude Code.

## Current state

<!-- handoff:state:start -->
| | |
|---|---|
| **Branch** | `claude/degenaration-launch-remediation` |
| **Branch base** | `2f03090` — **71** commits landed since |
| **Suite** | `npm run check`: **122 tests passed, 0 failed** |
| **Migrations applied** | see DATABASE_AUDIT.md |
| **Requirements** | 15 PASS · 2 PARTIAL · 0 BLOCKED |
| **Unpushed commits** | 1 |
| **Uncommitted files** | 2 |

_Generated by `npm run handoff` — do not edit this table by hand._
<!-- handoff:state:end -->

Codex usage limit resets **2026-08-05 13:30**.

## Codex: read this before doing anything

Your last session ended at `e68ac3f` and reported "41 server tests passed". That number is
now **110**, and 45 commits have landed since. If you resume from your previous context you
will be working from a picture that is roughly 40 commits out of date.

Orient with:

```bash
git log --oneline e68ac3f..HEAD          # what changed
npm run check                            # confirm the suite is green for you too
cat docs/coordination/IMPLEMENTATION_STATUS.md   # requirement status with evidence
```

`npm run codex:plans` extracts your own plan history into
`docs/coordination/CODEX_PLANS.md`, so Claude can see your direction. Refresh it after you
plan.

## DO NOT REVERT — decisions that look like bugs and are not

Each of these is deliberate, load-bearing, and covered by a test or a recorded reason.
Changing any of them re-breaks something that was fixed.

1. **The worker mirrors `PLATFORM_FEE_BPS` instead of importing `lib/fee-model.js`.**
   `render.yaml` deploys it with `rootDir: server`, so `lib/` does not exist at runtime.
   A cross-import would crash the deployed worker. A drift test fails if the values differ.

2. **`.app-shell` overrides the semantic colour tokens.** It scopes a slightly deeper gold
   (`185 139 93`) than `:root` (`194 148 99`). Collapsing them shifts every in-app surface.

3. **`toxic` / `hotpink` / `cyber` were removed from Tailwind on purpose.** They were named
   for a retired neon palette while holding warm gold values. Do not restore them; use the
   semantic names.

4. **`retained_fee_lamports` is a generated column.** Never write to it. The balance
   identity `creator + referral + retained === platform_fee` holds by construction.

5. **The `platform-allocation-offset` ledger entry must exist.** Without it the platform
   account is credited the gross fee while the creator is also credited, so a 200 bps fee
   produces 270 bps of credits and the ledger stops balancing.

6. **`body` is transparent; the canvas colour lives on `html`.** Moving it back to `body`
   hides `DegenBackdrop` behind it.

7. **`copy.js` is deliberately still on the platform safety baseline.**
   `copy_subscriptions` has no `extended_config` column, so there is no per-bot config to
   honour there yet. This is recorded, not an oversight.

8. **The two Discord sources showing dashes is CORRECT.** Live DB: `raw_signals`=0,
   `market_snapshots`=0, `durable_jobs`=0. The worker has never run. Replacing dashes with
   values would be fabrication — §9.6 and §23 forbid it. Deploying the worker is the fix.

9. **`PLATFORM_FEE_ACCOUNT` is intentionally unset.** The owner's wallet
   (`FSF99fXB…`) is **not** a valid Jupiter `feeAccount` — it is a wallet, not a token
   account, and does not exist on chain. Setting it would make every swap fail at
   execution. Jupiter's API accepts any value without validating it, so "the API accepted
   it" is not evidence. See B-1.

## Division of work

Per spec §0: **Claude implements, Codex reviews committed checkpoints.**

**Codex, when you return, the highest-value work is:**

1. **Independent review of the 45 commits** against the master spec, written to
   `docs/coordination/CODEX_REVIEW.md` with severity, file:line, reproduction, expected
   behaviour. You have not seen any of this work.
2. **The video-derived UI you planned and did not finish.** Your own plan history ends with
   "Implement simplified Discord/KOL setup and explicit Portfolio logout" at browser-QA
   stage, with "Commit the verified runtime changes and deploy production" never completed.
   You can watch the recordings; Claude cannot process video. This is genuinely yours.
3. **Deploy the worker** (B-2), which also closes requirements 6 and 7.

**Do not** start a competing implementation, and do not edit this working tree while Claude
is active. Review a committed checkpoint, or use a separate worktree
(`codex/degenaration-review`).

## Open blockers, owner-held

See `OPEN_BLOCKERS.md`. Summary: `PLATFORM_FEE_ACCOUNT` needs a Jupiter referral token
account (B-1), the worker needs signing config and a host (B-2), mainnet needs explicit
authorization (B-3).

## Uncommitted working-tree state, inherited and deliberately untouched

- `docs/activity-log.md` — modified; excluded from commits by policy
- `public/video/launch-source.mp4` — deleted by an earlier session; **not** committed,
  awaiting owner confirmation
- `tsconfig.tsbuildinfo` — build cache noise

## Exact next action for Claude

Requirements 3, 7, 19, 20 are PARTIAL on physical dependencies only (funded wallet,
deployed worker, signed-in session). Each has a command:
`npm run verify:withdrawal`, `npm run verify:discord-live`. Nothing further is reachable
without the owner acting.
