# DEGENARATION FINAL ONE-PASS GOAL

Continue from the current branch, commits, working tree, Vercel app, Supabase production project, Railway Discord listener, and existing `docs/ai` evidence.

RULES
- Do not restart, full-scan, repeat completed work, restate history, call Codex, or run dynamic/parallel workflows.
- Read only current Git state and relevant rows in IMPLEMENTATION_STATUS, OPEN_BLOCKERS, PENDING_DEPLOYMENT, FINAL_EXECUTION_HANDOFF, DISCORD_LISTENER_EVIDENCE, MIZAR_PARITY_MATRIX, CLICK_FLOW_MAP, and REFERENCE_COVERAGE.
- Open original media only for one undocumented detail.
- Do not present menus, wait idly, or stop after planning.
- Use a replay-safe stored event, deterministic automated-embed fixture, or approved test webhook instead of waiting for a random Discord call.
- Never fabricate data, use client funds, sign, or broadcast a funded transaction.
- Keep `mainnet_execution_enabled=false` until final approval.
- Per milestone: inspect relevant code → fix root cause → targeted tests → full check/build → browser proof → update evidence → commit → continue.
- Ask for one unavoidable owner action only. At approval gates, stop once; never loop.

1. DISCORD
Prove third-party bot/webhook embed → approved guild/channel → raw event → parse content/embeds/fields/buttons/links/attachments/edits → mint validation → scanner → call journal → source profile → immutable call-time price → subscriber config → durable intent.
Accept human, bot, and webhook messages; ignore only this app’s own messages. Support create/update/delete history, deduplicate by guild/channel/message, keep truthful quarantine reasons, and keep exactly one `/register`. Retire the legacy listener only after Railway is authoritative. Execution stays disabled.

2. MARKETPLACE
Show real server avatar, latest calls, freshness, token/mint, call price, market cap, liquidity, current/max return, `-50%`, `+50%`, `+100%/2x`, `+400%/5x`, best/worst, win rate, average/median, 1D/7D/30D, drawdown, followers, copied executions, confirmed volume, and history. Keep source-call performance separate from subscriber PnL. Unknown data must say Monitoring, Collecting data, or Insufficient history.

3. SETTINGS + AUTO-TRADING
Persist, reload, edit, version, validate, and enforce wallet, source/channel, buy size, capital/position/daily limits, slippage, priority fee, retries, cooldown, liquidity/market-cap/token-age/security filters, TP/sell %, SL, trailing TP/SL, DCA, pause/resume, and kill switch. Every intent stores the immutable settings snapshot.
Verify signal → eligibility → capital reserve → durable intent → quote → unsigned mainnet simulation → signing boundary → submission boundary → signature persistence → confirmation → execution → settlement → positions/lots → TP/SL/trailing/DCA exits → 2% fee → creator/referral allocation → reconciliation → Portfolio/performance update.
Use deterministic tests and unsigned simulation only before final approval.

4. MIZAR-FAMILIAR UI
Finish every FAIL/PARTIAL parity row. Match demonstrated navigation, structure, control placement, setup order, grouping, progressive disclosure, marketplace, source profile, Discord/KOL setup/editing, Bot Manager, Affiliate, Portfolio, Admin, dialogs, confirmations, and responsive behavior.
Preserve DegenAration logo and black/gold/white identity. Do not copy Mizar trademarks, logo, source code, exact text, or artwork. Remove generic AI cards, emojis, fake covers, random polygons, oversized empty panels, and walls of text. Use professional SVG icons, concise copy, tooltips, polished states, and tabular numbers.

5. PORTFOLIO, PNL, ADMIN
Finish authoritative balances, positions/lots, trade/deposit/withdrawal histories, 7D/30D/3M charts, realized/unrealized PnL, and winning/losing/Portfolio share cards with links, QR, and high-resolution export.
Admin must be server-restricted to the approved administrator and show each client’s balances, positions, deposits, withdrawals, PnL, bots, failures, fees, and confirmed volume today/7D/30D/lifetime. Never add balance editing.

6. DEPLOY + VERIFY
Do not leave verified fixes undeployed. Before irreversible actions show only target, SHA/function/migration, tests, expected data changes, visible effect, rollback, and whether funds can move. After approval, deploy sequentially and verify.
Run format, lint, strict typecheck, unit/integration/browser tests, production build, migration/RLS, secret scan, financial invariants, accessibility, authenticated browser proof, and responsive checks at 390/768/1024/1440. Fix BLOCKER/HIGH and relevant financial/security/data-integrity MEDIUM issues.

DONE means all internally solvable features are implemented, tested, committed, and deployed; Discord embeds reach journal/source profile; settings affect durable intents; Mizar parity is PASS or externally blocked; authenticated Discord/KOL/Bot Manager/Affiliate/Portfolio/PnL/Admin flows work; and remaining blockers are exact credential, signer/worker, fee-token-account, real-transaction, or irreversible approvals.

Begin at the first current failing dependency and continue sequentially.
