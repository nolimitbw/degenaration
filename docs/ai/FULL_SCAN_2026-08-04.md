# Full project scan — 2026-08-04

The §4 scan, performed once. Recorded so it is not repeated: re-scan an area only when code
in that area changes.

Method: enumerate, then check the thing that would fail silently. Inspection alone is not
evidence, so every finding below names how it was observed.

## What the scan looked for first

The root cause four times in this project has been **a table several surfaces read and nothing
writes**. It never raises — a left join renders a dash. So writer coverage was checked before
reader correctness.

| Table | Readers | Writers | Verdict |
|---|---|---|---|
| `trade_intents` | many | 1 | fixed `fcebe9c` |
| `trade_executions` / `positions` / `position_lots` | 4 surfaces | 1 | fixed `d554243` |
| `position_exits` | PnL card, admin, snapshots | 1 | added `a428857` |
| `signal_deliveries` | fan-out | 1 | fixed `5c1012a` |
| `performance_snapshots` | 4 surfaces | 1 | fixed `cbeabe5` |
| `cash_movements` | Portfolio, admin | **0** | **FAIL** — needs the withdrawal path to run (E-6/E-3) |
| `durable_jobs`, `worker_leases`, `scanner_tokens`, `scanner_pools`, `market_snapshots` | worker | 0 in repo | expected — written by the worker at runtime (E-3) |

## Financial

| Area | Status | How observed |
|---|---|---|
| Wallet registration, identity from verified session | PASS | `verify:wallet-registration`, 12 properties; migration deployed |
| Available / locked / pending / withdrawable | PASS | `verify:withdrawable-state`, 12 properties |
| Trade intents, capital reserved before commitment | PASS | `verify:trade-intent-fanout`, 12 properties |
| Abandoned-intent recovery | PASS | `verify:intent-reconciliation`, 5 properties |
| Executions, settlement | PASS | `verify:settlement-writer`, 8 properties |
| Positions, lots, partial exits, realized PnL | PASS | `verify:exit-settlement`, 13 properties |
| Withdrawal intents, idempotency, signatures | PASS (code) | `verify:withdrawal-idempotency`; local-validator transaction |
| Fees, commissions, referral rewards | PASS (code) | `verify:creator-referral`, `verify:fee-ledger`; collects 0 bps until E-4 |
| Payouts, reversals | PASS | `verify:payout-lifecycle` |
| Portfolio balances, chart, histories | PASS | equity series `b132a3f`; chart reads it |
| Deposits / `cash_movements` | **FAIL** | No writer. E-6/E-3 |
| Float money on the server | 1 accepted boundary | `server/engine/jupiter.js:168` floors a legacy `double precision` SOL amount into lamports. Same legacy-boundary pattern as the intent trigger; floored, never re-multiplied |

## Trading

| Area | Status | How observed |
|---|---|---|
| Discord commands, uniqueness | PASS (offline) | `check:discord-commands`; live state needs E-2 |
| Ingestion entry point, dedupe, parser version | PASS | `verify:discord-ingestion`, `verify:performance-journal` |
| Subscriber fan-out | PASS | `verify:signal-fanout`, 6 properties |
| Quote / simulate / sign / submit / confirm | code exists, **never run** | E-3 |
| Duplicate-signal and duplicate-trade protection | PASS | content hash; unique `entry_sig`; settle idempotent on `(intent_id, attempt)` |
| Worker leases, queues, retries, health | code exists, **never run** | E-3 |
| Staging / devnet proof | **BLOCKED** | §6 is explicit this is not PASS without it |

## User product

| Area | Status | How observed |
|---|---|---|
| Navigation is Bots / Affiliate / Portfolio | PASS | `components/AppShell.tsx` links only those |
| **Retired trading surfaces still resolved** | **was FAIL — fixed `b7ff573`** | 13 routes, zero inbound links, still served full pages. Now redirects; `check-code-quality` rule added with a control run |
| Decorative controls | PASS | 40 handler-less buttons inspected: every one is `disabled` with a truthful reason, which §9 permits. `check-code-quality` rule 7 enforces it |
| Public copy | PASS | `check:visible-copy` clean across 186 files |
| Loading / empty / error / provider states | PASS | allSettled + timeout + retry + stale banner on Affiliate, Portfolio, KOL |
| PnL cards, winning / losing / portfolio | PASS (code) | losing card unblocked `261bab4`; render evidence needs E-6 |
| Responsive, accessibility | PARTIAL | 390/768/1024/1440 evidence exists for builder and marketplace; authenticated surfaces need E-6 |

## Admin

| Area | Status | How observed |
|---|---|---|
| Authorization, three-deep | PASS | `verify:admin-client-ledger`; anon and authenticated denied; non-admin actor rejected; control run confirms the actor check fires |
| Client table | PASS | mounted `370e07d` — it existed and was imported by nothing |
| Client detail | PASS | `7c4397a`, 7 properties |
| Volume definition | PASS | one predicate across today / 7D / 30D / lifetime; control run proves a failed execution is excluded |
| Performance refresh | PASS | `admin_refresh_performance`, Clients tab |
| Audit log, source actions, incident controls | PASS | existing console sections |
| No balance editor, no secrets | PASS | deliberate; custody model stated on screen |

## Release

| Gate | Status | How observed |
|---|---|---|
| Typecheck, lint, tests, verifiers, build | PASS | `npm run check` exit 0 — 180 tests, 24 verifier suites |
| Bridge contract | PASS | 72 operations, every reference reachable, every parameter list matches its SQL signature |
| Migrations | 10 unapplied | `PENDING_DEPLOYMENT.md`, mandatory order recorded |
| RLS / grants | PASS | no `app_private` grant to `anon` or `authenticated` |
| Secret scan | PASS | no key, token or credential pattern in tracked files |
| Rollback | PASS | recorded in every migration header |
| Deployed edge function | **stale** | v11; repository is v12 |

## Conclusion

One product defect was found by this scan and fixed in the same pass: thirteen retired trading
surfaces that navigation and robots.txt had stopped advertising but that still served working
pages.

Everything else is either passing with a verifier, or waiting on E-2, E-3, E-4, E-6, or the
owner's approval of the deployment package. There is no remaining internally-solvable
requirement.
